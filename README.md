# Rxb06 Tap

## How do I install these formulae?

`brew install rxb06/tap/<formula>`

Or `brew tap rxb06/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "rxb06/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
